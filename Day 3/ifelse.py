# Basic Syntax
if condition:
    #* do this
else:
    #! do this

water_level = 50
if water_level > 50:
    #* do this
else:
    #! do this

# Nested
Basic Syntax
if condition:
    if another condition:
        #* do this
    else:
        #! do this
else:
    #! do this